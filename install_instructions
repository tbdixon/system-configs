# curl -sL install-node.now.sh/lts | bash
# cp .alias.sh $HOME
# cp .bashrc $HOME
# source $HOME/.bashrc
# source $HOME/.alias.sh
# cp .vimrc $HOME
# sudo pip install jedi
# sudo brew / apt install clangd (https://clangd.llvm.org/installation.html READ THESE INSTRUCTIONS)
###### inside vi
# :CocInstall coc-clangd
